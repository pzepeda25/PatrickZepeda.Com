# Give the Lead Hunter engine priority (Source -> Destination -> Status)
/.netlify/functions/* /.netlify/functions/:splat  200

# Send everything else to the React App (Single Page App routing)
/* /index.html  200